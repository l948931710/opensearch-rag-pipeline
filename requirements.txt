# ═══════════════════════════════════════════════════════════════
# OpenSearch RAG Pipeline — SAE 代码包部署依赖
# ═══════════════════════════════════════════════════════════════

# Core
pypdf>=4.0
python-docx>=1.0
openpyxl>=3.1
requests>=2.31
Pillow>=10.0
PyMuPDF>=1.24
# 抽取链路真实运行依赖（此前漏声明 → 与 pyproject 漂移；两条部署路径实际都不读本文件
# [Dockerfile 装 pyproject、DataWorks 内联 pip]，但本文件被 README 标为部署清单，不能骗人）：
pdfplumber>=0.11        # PDF 版面/文本提取（pdf_extractor 主路径；漏则 PDF 抽取降级）
python-pptx>=0.6        # PPTX 提取（_extract_pptx；漏则 pptx 静默空抽）
jieba>=0.42             # 中文分词（image_relation_classifier / chunker 图文关系分类）

# API Runtime
fastapi>=0.115
uvicorn[standard]>=0.30

# Alibaba Cloud Production
pymysql>=1.1
DBUtils>=3.0
opensearch-py>=2.4
oss2>=2.18
dashscope>=1.14
alibabacloud-ha3engine-vector>=1.1.18

# DingTalk Stream 模式（出站 WSS 收机器人消息/卡片回调，免公网回调端点；
# 由 DINGTALK_STREAM_MODE=true 启用，见 dingtalk_stream_runner.py）
dingtalk-stream>=0.24
