__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/
.pytest_cache/
.ruff_cache/
.env
.env.local
.env.local_ab_*
.env.prod_ro
.env.staging
# 模板（仅变量名）显式放行可提交
!.env.production.template
*.log
index-jobs/
rag-ready/
processing/
fuling_chunk_exp/
scratch/
recovered_pipeline_nodes.py

# Generated reports & assets
work_report*.md
work_report*.svg
工作汇报.pdf
convert_report.js
node_modules/

# mermaid 图渲染产物 (由 `make diagrams` 从 .md 的 mermaid 块重建,不版本控制)
docs/diagrams/

# Deployment build artifacts (rebuilt on demand, not version-controlled)
*.zip

# Moved-aside logs (see logs/)
logs/
.DS_Store
.env.test
.env.production

# MCP config — contains a live Alibaba AccessKey secret; never commit
.mcp.json

# eval_harness run artifacts (bulky/ephemeral — the harness CODE is tracked,
# but per-run reports, bundles, shards and caches are regenerated on demand)
eval_harness/reports/run_*/
eval_harness/reports/shards/
eval_harness/reports/active_bundle.json
eval_harness/reports/rerank_ab.json
eval_harness/reports/rerank_ab_v2.json
eval_harness/reports/rerank_smoke.json
eval_harness/**/__pycache__/

# Claude Code 项目本地指令 (含 RDS 端点等内部细节,不入远程)
CLAUDE.md

# Ad-hoc PDF viewer / extractor 残留图片 (仓库根目录)
/pdf_*_img*.jpeg
/pdf_*_img*.jpg
/pdf_*_img*.png

# Claude Code 本地工作区 (skills/launch.json 等,不入远程)
.claude/

# 评测模板/数据 — 本地素材,不入远程
一期模型评测集和评测模板_v2.0(1).xlsx
