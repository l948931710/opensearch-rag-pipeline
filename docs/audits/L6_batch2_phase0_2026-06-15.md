# L6 rollout Batch 2 — Phase 0 (read-only) — GREEN, awaiting reset→Stage 2 authorization

2026-06-15. Selected 100 of the remaining 217 affected docs; frozen-routing manifest + offline dry-run.
No production writes.

## Selection (deterministic, dept × file_ext stratified)
remaining = 217 (277 − 10 canary − 50 batch1). Selected **100** by round-robin across (dept, file_ext):
- dept: ADMIN 20 / HR 30 / QUALITY 15 / RD 14 / PRODUCTION 10 / MARKETING 8 / FINANCE 3
  (IT/SUPPLY/PMC exhausted in canary+batch1).
- file_ext: docx 78 / xlsx 10 / pdf 9 / pptx 2 / png 1 (good type diversity — adds pdf/pptx/png).
- frozen mode mix: text 45 / clause 44 / step 11. `scratch/l6_ab/batch2_docs.json`.

## Pre-flight (RDS) — all PASS
- current-version 100/100 `content=DONE chunk=DONE status=active`; **0 not-DONE, 0 no-canonical, 0 stale PROCESSING**.
- corpus **stage-2 pending = 0, stage-3 pending = 0** (clean queue → reset scopes to exactly these 100).
- before-total (RDS active == rebuild): **920**.

## Frozen routing manifest (from HA3 serving)
`scratch/l6_ab/batch2_routing_manifest.json` · 100 docs · **SHA256
`b1f70e7d9a4b79a6415156a727d6f5b49f5613ca0aad6e22327aa355e170a78c`** · HA3 rebuild total **920 ==
before-sum** (RDS==HA3, never re-chunked) · **0 unknown-mode**.

## Offline dry-run (OSS read + in-memory chunk, no writes)
**Predicted total 920 == rebuild 920 — EXACT MATCH, 0 mismatched docs**; every doc count + type mix ==
frozen manifest. `scratch/l6_ab/batch2_dryrun_prediction.json`.

### Fix A / Fix B impact (new vs RDS rebuild, matched 920/920 by chunk_id)
- **Fix A** (`[上文]` removed): **326 chunks / 44 docs**.
- **Fix B** (section_title changed): **382 chunks / 97 docs**.
- docs touched by Fix A or B: **100 / 100** (all are affected-set members, as expected).
- **Anomaly docs: 0** (no count/type mismatch, no unknown-mode).

## Code provenance
HEAD `80d26ca`; the 3 rollout fixes present (`844483e` reset / `edbe1ef` strand / `e0146db` maintenance);
chunker.py + pipeline_nodes.py + reindex_states.py committed/clean. **One unrelated uncommitted file:
`opensearch_pipeline/extraction/docx_extractor.py`** (a separate docx-Subtitle extraction fix) — it is
**NOT in the batch-2 re-chunk path** (stage-2 reuses existing canonicals; no re-extraction), so it is
inert for this batch. Flagged for clean provenance before the prod write.

## Verdict
Phase 0 GREEN. Awaiting separate authorization for reset → Stage 2 (maintenance mode, frozen manifest
`b1f70e7d…`) on these 100 docs. No prod write / embed / push / purge performed.
