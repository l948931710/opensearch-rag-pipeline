# L6 prod canary — Step 2 (embed native dense+sparse + push 374) — COMPLETE, no purge

2026-06-15 ~20:28. Authorized scope: embed + push the **374 new chunks of the 10 canary docs only**;
no deactivation, no purge, no version switch. JSON record: `L6_canary_run_2026-06-15T2028.json`.

## Preview gate (read-only) — GREEN
Corpus-wide stage-3 claimable set = **exactly 374, all canary, 0 non-canary** (clean corpus). The
scoped loader additionally hard-wires `doc_id IN (canary 10)` and asserts `== 374` before running, so
scope is guaranteed *by construction* (constraint #1/#2), independent of corpus state.

## Precondition fix (scoped, 10 docs)
First Stage-3 attempt **skipped all 10** — root cause: the Step-1 reset set
`document_version.index_status='NOT_STARTED'`, but the stage-3 lock predicate needs `'NOT_INDEXED'`
(the chunk_meta loader keyed on `chunk_meta.index_status='NOT_INDEXED'` worked; the dv-lock didn't).
No work, no writes (`dag3_no_work`, 0 rows locked). Corrective: scoped flip `NOT_STARTED→NOT_INDEXED`
on the current version of the 10 docs (preview=10, committed 10, `ack=PROD-RW:2026-06-15`), then re-ran.
**→ Process fix for the full 267-doc run: the reset must set `index_status='NOT_INDEXED'` directly.**

## Embed + push (prod write) — clean success
| | |
|---|---|
| Embedding | model `text-embedding-v4`, **native dense+sparse** (cache cleared → 374 cache-miss) |
| Embed result | **success 374 / fail 0 / retries 0**; dense dim 1024 present ×374; sparse present ×374 |
| Push | HA3 table `fuling_kb_chunks`, 7 bulk batches, **indexed 374 / failed 0 / errors False** |
| Deactivation (node 05) | **0 HA3 deletes, 0 RDS deactivations** ("No older chunks found" ×10); same-version → `_search_delete_old_chunks` early-return; only flipped `document_version→SUCCESS` |

## Read-only verification (isolated by NEW PK = `chunk_meta.id`; old orphans excluded)
| dim | result |
|---|---|
| **DIM1 RDS** | 374 `INDEXED`, `indexed_at` set; section_title>255 = 0; `[上文]` in text = 0. `opensearch_doc_id` stores the string `chunk_id` (by design); HA3 PK = int `chunk_meta.id`. **PASS** |
| **DIM2 reconcile** | per-doc HA3 total == 2× cm count (clean 1:1 old:new); **new-in-HA3 = 374, missing = 0**; 374 old orphans (pre-purge). **PASS** |
| **DIM3 sparse** | **374/374 (100.0%) non-empty sparse, 0 `[0]`-fallback** — the required gate. **PASS** |
| **DIM4 dense** | 374/374 dim==1024. **PASS** |
| **DIM4b self-retrieval** | 6/6 sampled new chunks rank-1 (order=DESC) over their old twin. **PASS** |
| **DIM6** | deactivation 0; 374 old PKs still serving (not purged). Expected pre-Step 4. |

DIM5 end-to-end (rerank path) is **deferred to post-purge Step 5** — during coexistence the retriever
returns both old+new twins (duplicate recall), which would confound an e2e signal; cleaner after purge.

## Verdict
**Step 2 PASS.** 374 new chunks indexed with full native dense+sparse, fully reconciled, 100% sparse
coverage; the 374 old chunks remain intact and serving (no purge). Awaiting Step 3 authorization.
