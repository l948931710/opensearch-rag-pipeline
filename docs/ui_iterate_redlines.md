# console UI/UX 迭代红线登记表(living registry)

`/ui-iterate` 的 ui-auditor 开工必读:**命中本表的问题不得再作为发现上报**(最多末尾一行带过)。调度器在每轮收尾时把新的用户拍板追加到这里(注明日期)。本表只收「用户已裁决」与「已证伪的审计结论」,不收普通遗留问题(那些留在各轮报告里)。

## WONTFIX / 不做(用户拍板)

- **375px / 移动端适配**(2026-07-04):console 不做手机端,手机场景由钉钉小程序承接;<640px 窄屏兜底提示隐藏遵循同惯例。平板/触屏 `hover:none` 的可用性修复**保留**。
- **假阶段状态条**(2026-07-14 评审红线)。
- **审批卡内嵌 diff**(2026-07-14):属 PR-4 范围,console 轮次不做。
- **批量审批**(2026-07-14)。
- **聊天内消歧**(2026-07-14)。
- **通知原提问员工 / 知识缺口人工状态 / 复核打卡 DDL / 审批历史高级搜索**(2026-07-04 反馈闭环批,用户拍板不做)。
- **审批历史分页**(2026-07-14 批次γ):有意出批——多源聚合分页需后端设计,不在 console 轮次里做。

## 已证伪的审计结论(勿再采信)

- **「驳回类操作无确认」**(2026-07-04):审计误读——ApprovalQueue/AccessRequestQueue 的 onReject 都走 promptText danger 弹窗。
- **QA 空态 chip 行对齐**(2026-06-30):逐像素证实 near no-op(3 居中 chip Δ=0);**别改 justify-start**(更不平衡)。

## 待用户拍板,勿自行推进

- **品牌字形变更**('+'/AI 头像标识歧义、Sparkles→Sprout 等,2026-06-30 / 07-03):任何品牌字形改动都须用户最终拍板,不得作为迭代改动顺手做掉。

## 口径 / 有意设计约束(涉及时就地标注,不当问题报)

- **同名指标两套口径**(2026-07-14 批次γ):governance「问答 API 成功率」=实时 30 天滑窗且 SQL 显式**排除** `model_name='agent'` 行;`qa_daily_metrics` 的 error_rate=qa_rollup 按北京日物化且**含** agent 行——同名指标两套算法必然对不上,任何看板复用这两源必须就地标注口径。三个 p95 各不同体:qa_session_log 端到端 / llm_call_log 调用粒度 / qa_daily_metrics 物化。
- **DeptTable 固定 30 天口径**(2026-07-14 δ-2):表格有意不随 7/30 天切换(排序/无答案率均 30 天口径),切换只作用于卡片;列头「30 天」hint 即为此设。
- **本体工作台空页仍返回游标**(2026-07-14 δ-1):cursor 编码在管辖精判之前,dept_admin 可能收到空页但仍有游标——「继续加载」语义,非终止,非 bug。
- **贡献页 loadGaps 回首页重置翻页态**(2026-07-14 批次α):有意。
- **browse.spec 断言 aria-label「退役下线/恢复上线」勿改**(2026-07-04)。
