# `RAG_ACL_ANCESTRY` 翻闸记录 · 2026-08-05

> 变更:现网 `RAG_ACL_ANCESTRY` **OFF → ON**(Sam 2026-08-05,SAE 环境变量)。部门→ACL组
> 解析从「名字表键控」改为「dept_id 锚 + 最近祖先制」。
> **不随 08-05 镜像发布**——镜像(`f918e37`)只是把锚表新值带上线,锚表整体一直被本 flag 门控;
> 发布记录 `docs/ops/image_release_2026-08-05.md` §4 明确标注过"不是本次发布的效果"。
>
> 证据:`scripts/verify_acl_coverage.py --ancestry`(prod 只读,零写入)+ 本文各节的
> `prod_access.get_prod_readonly_conn()` 逐项核查。组织快照时点:dept_dim `synced_at
> 2026-08-04 09:16`(org_sync 日调 00:15)。

## 1. 结论(TL;DR)

**无组员工 279 → 81(23.9% → 6.9%),`supply` 零受众告警消失。** 代价是一次实际的授权扩张:
`production +111 / overseas +73 / admin +63 / quality +24 / pmc +15 / supply +15 /
marketing +9 / hr +8 / it +4 / legal +1`。

扩张的**绝大部分是修复**(把漂移吃掉的权限还回去),但有一块是**实质新增可见面**:
**39 名海外系员工现在可读 709 篇大陆生产 dept_internal 文档**(§3 待确认项)。

## 2. 翻闸前后对照

| | 名字制(翻闸前) | 祖先制(翻闸后) |
|---|---:|---:|
| 在职员工 | 1167 | 1167 |
| **无 ACL 组** | **279** | **81** |
| 零受众告警 | `supply` | 无 |
| 残余分布 | 11 棵顶层树 | **2 棵**(其他 70 / 获胜包装 11) |

死键 9 个(翻闸后全部失效,因锚按 dept_id 键控、改名免疫):`资材部`(→采购部)、
`自动化信息部`(→信息部)、`获胜工厂`、`印尼公司`、`墨西哥公司`、`海外生产中心国内办公室`、
`法务`、`工程`、`PMC部`。

## 3. `production +111` 的顶层子树构成

| 顶层子树 | 新获 production | 定性 |
|---|---:|---|
| 生产中心 | **62** | ✅ **纯修复**。采购部改名 + 薄膜家族 + 新叶继承,本就该有 |
| 获胜包装 | **34** | ⚠️ 获胜生产中心 33 + 直挂 1。锚 `[overseas,production]`(07-03 口径,08-04 补锚)。**此前因漂移读不到,现为实质新增** |
| 营销中心 | 9 | ✅ 叶子继承父部级锚,见 §4。杭州分公司 5 / 外贸监装 2 / 内销监装 2 |
| 海外中心 | 3 | ⚠️ 海外服务部新叶,同获胜口径 |
| 墨西哥富岭 / 印尼富岭 | 各 1 | ⚠️ 已升顶层树,08-04 补锚 `[overseas,production]` |
| 综合管理中心 | 1 | 直挂人员 |

**三分法:纯修复 62 + 叶子继承 9 + 海外系实质新增 39(34+3+1+1)。** 前两类无争议;
第三类是本次唯一需要业务确认的:海外子公司员工读大陆生产 SOP 是否为预期。
锚值若要改,改 `dept_ancestry.ANCHOR_GROUPS_BY_DEPT_ID` 的
`1091525269`(获胜生产中心)/`488554573`(印尼)/`488669494`(墨西哥)三条。

## 4. 营销中心那 9 人的父链追溯(排除"祖先制副作用"猜想)

现查三条叶子的父链,**各一跳即命中显式锚**:

| 叶子 | 父部门(最近锚) | 锚值 |
|---|---|---|
| 外贸监装 (#631109077) | 国际贸易部 (#34301155) | `["marketing","production"]` |
| 内销监装 (#821792930) | 国内营销部 (#149975081) | `["marketing","production"]` |
| 杭州分公司 (#823034454) | 电子商务部 (#599754116) | `["marketing","production"]` |

锚表该段注释即「营销中心:中心级兜底 + 部级细化(部级更近 → 叶子拿部级值)」——
**营销三部带 `production` 是翻闸前就存在的既有拍板**,今天没有改变它。这 9 人此前无组,
仅因名字表里没有这三个**叶子**的 key;祖先制让它们继承父部值,正是 060 设计要的
「新建叶子部门自动继承」。⇒ **机制按设计工作,不是新增风险面。**

## 5. 残余 81 人(翻闸也救不回)

| 顶层子树 | 人数 | 定性 |
|---|---:|---|
| 其他 | 70 | **组织数据问题,非代码问题**。钉钉树里字面叫「其他」的部门,无职能语义 ⇒ 任何锚都无法自圆其说。根治=HR 归到真实部门;权宜=个人级 `user_role` seed |
| 获胜包装 | 11 | **设计如此的 fail-closed**。直挂海外树根;锚表有意不给树根设锚(「树根是公司语义、职能不明」,在根设锚会把 89 人一次性双职能授权)。出路=挂到已补锚的获胜生产中心/获胜行政中心,或个人级 seed |

个人级授权覆盖有限:`user_role` 仅 **75 行**有效(26 dept_admin + 2 kb_admin + 47 employee),
能对上在职员工 73 人且多数本就是管理员 ⇒ 这 81 人没有被个人授权悄悄兜住。

## 6. 待办

- [ ] **业务确认**:39 名海外系员工 × 709 篇大陆生产 dept_internal 文档(§3)
- [ ] **提 HR**:「其他」部门 70 人归口到真实部门——此项**与翻不翻闸无关**,一直存在
- [ ] 建议获胜把直挂树根的 11 人下沉到已补锚的两条职能线
- [ ] 语料真空期结束(重灌收敛)后复跑一次 `verify_acl_coverage.py`:届时
      `readable_dept_internal_docs` 才有意义(当前 overseas/it/legal/audit/corn_eco/
      engineering 六组均为 0,是语料问题不是权限问题)

## 7. 更正记录

1. **「营销中心 9 人是祖先制副作用、没人显式决定」——已撤回**(2026-08-05 当场)。
   §4 的父链追溯证明三条叶子各一跳命中**显式**的部级锚,`production` 是既有拍板值。
   初判错在只看了"名字制没有、祖先制有"的差集,没去追锚从哪来。**差集只说明变化,
   不说明成因**——追溯到锚才是权威。
