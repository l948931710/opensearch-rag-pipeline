# 阿里云工单草稿：HA3（OpenSearch 向量检索版）可检索索引静默丢失（DRAFT，Sam 提交）

**实例**：ha-cn-kgl4slr1n01（public endpoint ha-cn-kgl4slr1n01.public.ha.aliyuncs.com）
**表**：fuling_kb_chunks
**严重级**：高（生产语料 ~37% 检索失明 5 天）

## 现象

2026-07-06 16:30–17:40 与 07-07 10:30–11:00 两个窗口批量实时推送（cmd=add，合计
~10,366 文档行，批大小 8–100）。推送 API 全部成功返回；随后：

1. **按主键 filter 点查（`filter="id=<pk>"`）可取回完整字段**（正文/属性都在）——
   doc store 层数据完好；
2. **同一行在向量检索与文本倒排检索中均不可召回**：
   - 用该行存储正文自身做查询向量 kNN——不返回该行（抽样 15/15 复现）；
   - 用该行正文中的唯一文号（如 "FL-QC-015-002"）做文本检索——返回 11 个含同号的
     其它文档行，唯独不含该行本身；
3. 零向量 range-scan 枚举（`filter="id>=lo AND id<hi"`）能列出这些 id；docCount 与
   我方 RDS 侧行数一致（27,457）。

即：**doc store / 属性可达，倒排 + 向量索引不含这批行**。未执行过任何删除。

## 与既往事件的关联

2026-07-06 上午同实例发生过 69 行「docCount 在、按 id 点查不到」（时间窗内无删除
操作；其中 6 行是当天 09:15 推送并验证过的）——疑似实时段构建/合并异常的另一形态。
本次变体点查可达但索引不可达。两窗口共同点：短时间大批量实时推送。

## 请求

1. 请排查该表 2026-07-06 16:00 起的实时段构建/合并日志与段状态：这批行是否停留在
   未构建索引的实时段/被合并丢弃；
2. 给出根因与修复（我方可配合重推，但需要确认重推不会再次丢失）；
3. 若为已知问题请告知规避参数（推送速率/批大小/段合并配置）。

## 附样本（可复现）

- 点查可达、检索不可达行样例：id=83138（doc_id=DOC_QUALITY_20260611201419_5A271A，
  正文含 "FL-QC-015-002"，permission_level=public）；
- 对照健康行：id=21029（同表 6-15 推送代，自查询/文号检索均正常）；
- 受影响行清单（10,366 个 id）可按需导出提供。
