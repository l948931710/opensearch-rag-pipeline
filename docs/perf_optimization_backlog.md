# 性能优化 Backlog（全仓审查产出，共 97 项）

> 来源：2026-07-01 全仓架构与代码质量审查工作流（17 审查员 + 对抗验证）。见 [architecture_quality_review_2026-07-01.md](architecture_quality_review_2026-07-01.md)。

> 每项含 `file:line`、优化点、预期收益（依据从代码结构推导，非实测）、工作量 S/M/L。热路径本质是严格串行链，无"可并行的串行 await"大鱼——杠杆集中在**索引、连接复用、把簿记移出响应路径**。

> 状态标注：✅=已在修复批次中落地；其余为待办。分桶仅为阅读方便，不代表实施顺序（实施优先级见报告第六节的三梯队）。


**工作量分布**：S 级 70 项 · M 级 27 项。


## A. 数据库索引 / sargable 查询（收益随表增长单调放大）

| # | 位置 | 优化 | 预期收益 | 工作量 |
|---|------|------|----------|--------|
| 1 | `opensearch_pipeline/api.py:1323` | **hot-questions 与 rephrase 问题池对 qa_session_log 做无索引全表扫描（created_at/answer_status 无索引）**<br>hot_questions（1323行）与 _success_question_pool（1393行）都是 `WHERE answer_status='SUCCESS' AND created_at >= NOW()-INTERVAL 30 DAY AND CHAR_LENGTH(query_text) BETWEEN ... AND user_id NOT LIKE ... GROUP BY query_text | 随语料日志增长（数十万+行）单次冷查询从秒级降到走索引的百毫秒级；缓存失效时刻不再长时间占用线程池线程。 | M |
| 2 | `opensearch_pipeline/api.py:4739` | **/api/kb/gaps REFUSAL 查询用 JSON_TABLE + CONVERT(...COLLATE) JOIN，摧毁 document_meta.doc_id 索引**<br>REFUSAL 缺口查询（4739-4756行）对 qa_session_log.retrieved_docs_json 做 JSON_TABLE 展开后 `JOIN document_meta m ON m.doc_id = CONVERT(jt.doc_id USING utf8mb4) COLLATE utf8mb4_unicode_ci`。JOIN 键上的 CONVERT+COLLATE 强制类 | 消除大表 nested-loop 全扫，缺口看板查询从潜在秒级降到走索引的合理区间；降低 kb_admin 打开概览页的阻塞时间。 | M |
| 3 | `opensearch_pipeline/api.py:1974` | **看板归属链每次实时 JSON_TABLE 打散 retrieved_docs_json 再跨库 JOIN —— 6 处热查询无法利用任何索引，可物化为 (message_id, doc_id) 事实行**<br>qa_logger.log_qa_session 写日志时已在 Python 内持有 retrieved_docs 列表（qa_logger.py:157-171），顺手批量 INSERT 一张瘦表 qa_retrieved_doc(message_id, doc_id, created_at)（带 (doc_id, created_at) 与 (created_at) 索引，显式 | 归属类查询从「窗口全行 × JSON 解析 × 函数 JOIN」变为普通索引 JOIN；同时消除对 JSON_TABLE collation 陷阱的持续依赖面。与 perf#1 叠加后看板查询与日志表体量解耦。 | M |
| 4 | `opensearch_pipeline/api.py:4750` | **kb_gaps REFUSAL 查询 JSON_TABLE 展开 + COLLATE 转换使 doc_id join 无法走索引**<br>kb_gaps（4739-4756）用 JSON_TABLE 展开 retrieved_docs_json 再 join document_meta，join 键做 `CONVERT(jt.doc_id USING utf8mb4) COLLATE utf8mb4_unicode_ci`——对 join 列做函数/字符集转换会让 m.doc_id 索引失效、退化为嵌套扫描；该端点无缓存、每请求执行。建议统一两表 doc_id 字符集/排序规则以去掉 p | 让 document_meta.doc_id 主键/唯一索引可用，REFUSAL 缺口查询从嵌套扫描降到索引查找，缩短「缺失知识」页响应。 | M |
| 5 | `opensearch_pipeline/api.py:4221` | **document_version 按 raw_key（VARCHAR(1024) 无索引）等值查找 —— 上传注册/贡献物化的幂等检查每次全表扫**<br>raw_key_hash CHAR(64) 已在三条写路径落值（api.py:2878/4210、pipeline_nodes.py:568，注释明言「为未来的 UNIQUE(raw_key_hash) 加固预填数据」）：先回填存量 NULL，再建 `INDEX idx_raw_key_hash (raw_key_hash)`（能 UNIQUE 更好），把 api.py:4221/2833/2901 的 `WHERE raw_key=% | 幂等查从全表扫降为点查；同时为「同 raw_key 不重复登记」补上真正的数据库级约束能力（当前只靠先查后插，存在竞态窗口——代码已用 1062 兜底但键在 (doc_id,version_no) 而非 raw_key）。 | S |
| 6 | `opensearch_pipeline/qa_rollup.py:217` | **qa_rollup 日汇总用非 sargable 谓词 DATE(CONVERT_TZ(created_at,...))=%s，索引加了也用不上**<br>把 `WHERE DATE(CONVERT_TZ(created_at, 'America/Los_Angeles', 'Asia/Shanghai')) = %s` 改写为对裸 created_at 的半开区间：在 Python 侧把北京业务日转换成太平洋时间上下界（zoneinfo 处理 DST），`WHERE created_at >= %s AND created_at < %s`，与上条 created_at 索引配套。 | 日汇总从全表扫（函数包裹列使索引失效）降为单日范围扫；语义不变（同一 DST-correct 转换只是搬到常量侧）。 | S |
| 7 | `opensearch_pipeline/qa_rollup.py:217` | **qa_rollup 夜间聚合用非 sargable 谓词 DATE(CONVERT_TZ(created_at,...))=%s，永远吃不到索引**<br>在 Python 侧把目标北京日转换为太平洋时区的半开区间 [start, end)，改写为 `created_at >= %s AND created_at < %s`（DST 边界用 zoneinfo 计算，语义与 CONVERT_TZ 等价）。 | 夜间 rollup 从全表扫变为单日范围扫描；与 perf#1 的索引配套后才真正生效。 | S |
| 8 | `schema/002_feedback_system.sql:106` | **qa_session_log 缺 created_at（或 (answer_status,created_at)）索引，5+ 个端点/任务按时间窗全表扫**<br>用文件内现成的 `_add_index_if_not_exists` 存储过程（schema/002:149-151）给 qa_session_log 加 `INDEX idx_created (created_at)`（或复合 (answer_status, created_at)）。现状仅有 idx_session/idx_user/idx_message_id（schema/002:106-108），而 hot_quest | 每条上述查询从 O(全表) 扫描降为 30 天范围索引扫；qa_session_log 逐问答追加、无清理任务，扫描成本随上线时间线性增长——这是唯一会持续变慢的服务侧查询族。 | S |
| 9 | `schema/002_feedback_system.sql:106` | **qa_session_log 无 created_at 索引：Phase E 看板/缺口全部 30 天窗口查询走全表扫**<br>按既有 gated-DDL 纪律加 INDEX (created_at) 与复合 (answer_status, created_at)（沿用 002 的 _add_index_if_not_exists 守卫过程）。 | 所有窗口查询从 O(全表) 降为 30 天范围扫描；表随每日问答持续增长，收益随时间单调放大——这是这批看板端点里杠杆最大的单个 DDL。 | S |
| 10 | `schema/002_feedback_system.sql:106` | **qa_session_log 无 created_at 索引 —— 11+ 处窗口查询（console 看板/缺口/rollup）对无界增长的日志表全表扫**<br>加 `ADD INDEX idx_created_at (created_at)`（InnoDB online DDL）。命中点：api.py 2243/2273/2463/2470（governance 延迟、状态分布、可用性、24h 错误）、1980/2063/2390/2405（JSON_TABLE 归属查询的窗口前置过滤）、4724/4752（kb_gaps），共 11 处 `created_at >= DATE_SU | 窗口查询从全表扫降为 30 天范围扫；收益随表增长线性放大（无留存策略下这是唯一止血阀）。依据：上述行号的谓词形态 + 002/006 的完整索引清单。 | S |
| 11 | `schema/002_feedback_system.sql:106` | **qa_session_log 加 (answer_status, created_at) 复合索引**<br>幂等 DDL 增加 idx_status_time (answer_status, created_at)。 | 四处聚合从全表扫描变索引范围扫描；表增长到几十万行后看板加载与缓存冷启动从秒级降回毫秒级，且不再与问答落库写入争 IO。 | S |

## B. 连接 / 客户端复用

| # | 位置 | 优化 | 预期收益 | 工作量 |
|---|------|------|----------|--------|
| 12 | `opensearch_pipeline/embedding_client.py:88` | **模块级 requests.Session 复用 DashScope 连接（embedding/rerank/LLM 三处）**<br>各模块建线程安全的模块级 Session（配 HTTPAdapter pool_maxsize），requests.post → session.post。 | 消除每问答 2-3 次 TLS 建连（各约 3 个网络 RTT）与连接churn；摄取全量重灌少 ~1800 次握手。收益可用已落库的 retrieval_latency_ms/llm_latency_ms 前后对比验证。 | S |
| 13 | `opensearch_pipeline/pipeline_nodes.py:181` | **服务与批处理共用的连接池上限 10 且 blocking 无超时，看板类请求长占连接可饿死问答**<br>`maxconnections=10, blocking=True`（pipeline_nodes.py:181-182）由全部 serving 端点共享；kb_governance 单请求持 1 连接串行跑 ~14 条子查询（api.py:2215 注释『共享一个游标跑多条子查询』）。FastAPI def 处理器线程池默认并发 40：>10 个并发触库请求时其余线程无限期阻塞等连接、无超时无指标。建议：serving 进程调大 maxconnections（如 20-30，RDS  | 消除低概率但难诊断的『问答莫名卡住』尾延迟（现状饱和时无任何日志线索）；成本仅是几条空闲连接。 | S |
| 14 | `opensearch_pipeline/pipeline_nodes.py:181` | **连接池 maxconnections=10 与 RAG_CLASSIFY_CONCURRENCY=8 紧耦合，blocking=True 下调参会静默串行化**<br>maxconnections 随 RAG_CLASSIFY_CONCURRENCY 推导（如 max(10, concurrency+4)），或至少在池初始化日志里打印两者并在 concurrency>maxconnections 时告警。 | 消除『调大并发无效果』的隐形瓶颈；防止未来 429 缓解手段（提高并发+重试）被池上限抵消。来源：blocking=True 语义（line 183 注释）+ 两处默认值。 | S |

## C. 缓存（embedding / 权限 / 看板 / 问题池）

| # | 位置 | 优化 | 预期收益 | 工作量 |
|---|------|------|----------|--------|
| 15 | `opensearch_pipeline/api.py:2190` | **kb_insights / kb_governance 每次请求串行重算 4/13 条聚合子查询，无任何缓存**<br>加进程内 TTL 缓存（60-300s，key=角色+scope；--workers 1 单进程天然安全），或把日粒度指标改读已存在的 qa_daily_metrics 夜间物化表（qa_rollup 基建现成），只对『近 24h』类指标保留实时查询。 | 看板端点从每请求十余条聚合扫描降为缓存命中/单表点查；与 perf#1 叠加后 DB 端看板负载基本归零。 | M |
| 16 | `opensearch_pipeline/api.py:4739` | **kb_gaps 每个员工每次打开贡献页都全量重算缺口聚合（两条重查询 + Python 聚合），无缓存**<br>按 (排序后的 depts 元组) 为 key 做进程内 TTL 缓存（60-120s）缓存 open_gaps 全集，分页切片走缓存；或降频为每 N 分钟后台预计算。 | 同部门员工翻页/刷新时从两次重扫描降为内存切片；配合 perf#1 索引，页面延迟与 DB 负载双降。 | M |
| 17 | `opensearch_pipeline/dingtalk_bot.py:530` | **机器人每次问答都同步查 RDS 解析部门，无进程内缓存**<br>_process_rag_query 每条消息都调 _resolve_user_dept(staff_id)，即使命中 user_role 也要一次 DB 往返(dingtalk_identity.py:249)。加一个进程内 LRU + 短 TTL(如 60-120s)缓存 staff_id→组列表，热点用户免去每问一次 SELECT。 | 每条问答从关键路径上省掉 1 次 RDS 往返；高频用户/群聊尤其明显。TTL 同时顺带缓解上面『缓存永不刷新』的 P2（撤销/调岗窗口由 TTL 兜底）。 | M |
| 18 | `opensearch_pipeline/pipeline_nodes.py:5624` | **embedding 缓存是单体 JSON 文件（上限注释自述 ~220MB），整读整写且无 OSS 镜像，生产 serverless 上大概率永远冷**<br>现状：stage-3 每次运行 json.load 整个 `scratch/embedding_cache.json`（pipeline_nodes.py:5624-5637，注释『20000 entries ≈ … ≈ 220MB（JSON 文件上限）』），_save_cache（5652）全量重写。改为 sqlite/分片 KV 按需读写，并像 VLM 缓存那样加 OSS 持久镜像（CLAUDE.md：vlm_cache.jso | 去掉每次跑批的百 MB 级 JSON 解析/重写与常驻内存；OSS 镜像使生产重跑/重灌复用已算 embedding，直接省 DashScope 调用费（缓存命中路径代码已存在，只差持久层）。 | M |
| 19 | `opensearch_pipeline/pipeline_nodes.py:5632` | **embedding 缓存整文件 JSON 每个 drain 批次重复加载+整文件重写**<br>缓存提升为进程级单例（drain 循环在同一进程内反复调 run_stage）或改 SQLite/分片文件，仅追加写增量条目。 | 大缓存下每批省数秒 CPU + 数百 MB 峰值内存反复分配；也消除多批并发写同一文件时互相覆盖对方增量的浪费。 | M |
| 20 | `opensearch_pipeline/pipeline_nodes.py:5700` | **摄取 embedding 并发度按配额上调 + 巨型 JSON 缓存换 sqlite**<br>配额允许时 RAG_EMBED_CONCURRENCY 提到 8-10（纯环境变量）；缓存后端换 sqlite/分片文件，增量读写。 | embedding 阶段吞吐 +60-100%（并发 5→8-10，批大小已顶格无法再改）；省掉每 run 加载/序列化 100-220MB JSON 的数十秒与 2-3 倍峰值内存。 | M |
| 21 | `.github/workflows/ci.yml:29` | **CI Python job 无 pip 缓存，每次 push/PR 全量重装 production+ocr+api 依赖树**<br>actions/setup-python@v5 加 `cache: pip`（配 cache-dependency-path: pyproject.toml）。同一 workflow 的 frontend job 已在 :55 对 npm 做了缓存，python job 是明显的不对称。 | ci.yml:35 `pip install -e ".[dev,production,ocr,api]"` 包含 PyMuPDF、opensearch-py、oss2、dashscope、alibabacloud-ha3engine-vector、fastapi 等重依赖，每次 CI 全量下载安装；缓存命中后该步骤只剩本包 editable install，消除每次运行的大部分依赖安装耗时。 | S |
| 22 | `opensearch_pipeline/api.py:2038` | **kb_insights / kb_governance 看板每次请求现算 4~14 条聚合子查询（含 JSON_TABLE 展开 JOIN），无缓存**<br>复用同文件 hot_questions 已有的进程内 TTL 缓存模式（api.py:1307-1310 `_hot_questions_cache` + `_HOT_QUESTIONS_TTL_S`）：按 (endpoint, scope/owner_dept) 键缓存 60-300s。insights 每次跑 4 条、governance 约 14 条子查询，其中 JSON_TABLE 对 retrieved_docs_json | 看板打开/轮询时把重复的多秒级扫描降为 0（30 天窗口看板对分钟级 staleness 完全不敏感）；同时缩短单请求占用池连接（仅 10 个，见下条）的时长。 | S |
| 23 | `opensearch_pipeline/api.py:2010` | **kb_insights / kb_governance 每次请求串行跑 13+ 条聚合子查询、无任何缓存 —— 单 worker 上多管理员刷新即放大为重复全表扫**<br>看板是分钟级新鲜度可接受的聚合读：加一个进程内 TTL 缓存（key = role+scope_params，TTL 60-300s；--workers 1 使进程内缓存天然一致），命中直接返回上次 KbInsightsResponse/KbGovernanceResponse；或把整块聚合落到既有 qa_daily_metrics 模式的预计算表。 | 看板打开/轮询的 DB 负载按缓存命中率成比例下降（TTL 内多次刷新只打一次库）；对单实例共享连接池的串扰（看板扫描挤占问答路径连接）随之消失。 | S |
| 24 | `opensearch_pipeline/api.py:323` | **current_identity 每个已鉴权请求都打一次 DB（无进程内 TTL 缓存）**<br>RAG_LIVE_ACL_REREAD 默认开，current_identity 对每个带 Bearer 的请求都调 _resolve_user_dept_cached(uid)→_get_db_conn()+SELECT user_role（dingtalk_identity.py:317）后关连接，随后端点主体又各自 checkout 一次连接。热路径（/api/ask、/api/ask/stream、全部 /api/kb/*）每请求 ≥2 次连接池 checkout。加一个 30-60 | 去掉热路径每请求 1 次 pooled-conn checkout + 一次索引 SELECT；在并发上升时缓解 pool(max=10) 争用与尾延迟。 | S |
| 25 | `opensearch_pipeline/dingtalk_identity.py:314` | **current_identity 每次鉴权请求都打一次未缓存 DB 查询（函数名带 _cached 实则无内存缓存）**<br>_resolve_user_dept_cached 每次调用都 `_get_db_conn()` + `SELECT dept_code FROM user_role ... LIMIT 1`（314-325行），无任何进程内 memoization——'cached' 指的是 user_role 表是钉钉 API 的缓存，不是本函数缓存。而 current_identity（api.py 321-324行，RAG_LIVE_ACL_REREAD 默认 true） | 消除热路径上每个已登录请求的一次 RDS 往返（约 1-5ms/次 + 连接池占用），高 QPS 下显著降低 RDS 连接池压力与尾延迟。 | S |
| 26 | `opensearch_pipeline/dingtalk_identity.py:300` | **给读时 ACL 复核加 30-60s 进程内 TTL 缓存**<br>staff_id → (groups, ts) 的进程内 dict + 60s TTL + 容量上限；DB 失败沿用现有 fail-open。 | 每个鉴权请求少 2 次 RDS 往返；RDS brownout 时鉴权依赖不再逐请求卡 connect/read timeout（10/30s，config.py:178-179）。撤销窗口从“即时”变“≤60s”，仍远优于令牌 2h TTL，且 _deny_revoked_cross_dept 查询侧拒绝不受影响。 | S |
| 27 | `opensearch_pipeline/extraction/unified_extractor.py:1449` | **_save_vlm_cache 每处理完一个文档就全量 JSON 序列化 + 整包上传 OSS**<br>本地原子写保留每文档一次（廉价、防崩溃），OSS put 改为脏标记 + 每 N 个文档/run 结束时同步一次（orchestrator 已有 run 级 finally 位置）。与上面 P2 的 read-merge-write 修复可合并实施。 | 缓存已有 ~1500+ 条（本文件 137 行注释："OSS 上 ~1500 条"）且持续增长；100 文档的批次从 100 次全量 dumps+OSS PUT 降到个位数，省网络往返与序列化 CPU，也直接缩小 last-write-wins 竞态窗口。 | S |
| 28 | `opensearch_pipeline/retriever.py:89` | **重复问题每次都重新调 DashScope 算 query embedding，可加小 LRU**<br>get_query_embedding 每请求一次 DashScope native HTTP 调用（retriever.py:89 embed_texts_native([query])），无 memo。hot-questions 数据本身证明重复问题真实存在（api.py:1332 `HAVING cnt >= 2` 才入选）。加一个进程内 LRU（如 512 条，键=模型+原文，值=dense+sparse），单 worker 部署下天然一致。 | 重复/热门问题省掉一次外部 HTTP 往返（检索链路里唯一发生在 HA3 之前的串行外呼），对『示例问题快捷栏』点击这类必重复流量收益最直接；实现约 10 行。 | S |
| 29 | `opensearch_pipeline/retriever.py:1500` | **查询 embedding 无缓存：重复/FAQ 问题每次都打一次 DashScope**<br>retrieve_and_enrich 每请求调 get_query_embedding（1531 行文件的 1500 行），相同问题（FAQ 高频、会话内追问重发、评测重放）重复计算确定性 embedding。对 (model, dimension, query) 加有界 LRU（如 512 条，dense 1024 floats ≈ 4KB/条 → ~2MB 内存），multi-query 子查询同样受益（_one 内 idx!=0 各自再嵌入，retriever.py:1404-1407） | 命中时省一次 DashScope 往返（检索链路里唯一的第一跳外呼）及调用配额；FAQ 型流量占比越高收益越大。风险为零：embedding 对同一输入确定。 | S |

## D. 服务热路径 / 并发容量

| # | 位置 | 优化 | 预期收益 | 工作量 |
|---|------|------|----------|--------|
| 30 | `opensearch_pipeline/pipeline_nodes.py:642` | **node_extract_text_with_ocr 逐文档串行（OSS 下载+解析+OCR/VLM），而同管线 classify 已是 8 线程并发**<br>文档级 ThreadPoolExecutor（如 4 并发，参考 node_classify 的模式）：每线程独立子 tmp 目录、共享 cost_breaker 需加锁（或已线程安全则直接复用）；至少先做『下一篇 OSS 下载与当前篇提取重叠』的预取。 | stage-1 墙钟约按并发度缩短（IO/API 等待可重叠；来源：循环体内除共享 tmp_dir 外无跨文档数据依赖）。VLM funnel 内部虽有 8 并发，但仅覆盖单篇内图片，跨篇仍串行。 | M |
| 31 | `opensearch_pipeline/api.py:674` | **qa_session_log 写入移出响应关键路径（BackgroundTasks）**<br>log_qa_session 已吞异常、无返回值依赖（message_id 预生成），直接挂 FastAPI BackgroundTasks / 线程池执行。 | 从每次非流式回答的用户可见延迟中剔除掩码+落库耗时；RDS 抖动时 P99 不再被日志写入放大到 read_timeout 级别。 | S |
| 32 | `opensearch_pipeline/api.py:875` | **调大 AnyIO 线程令牌上限（或 async 化流式端点）**<br>启动时 `anyio.to_thread.current_default_thread_limiter().total_tokens = 100+`（一行）；中期 ask_stream 换 httpx.AsyncClient 异步流。 | 并发问答容量上限从 ~40 提到 100+（线程几乎全在等网络，仅付内存成本）；避免推广高峰/上游变慢时的整机假死。 | S |
| 33 | `opensearch_pipeline/dingtalk_bot.py:413` | **钉钉流式：每收一个 token 帧就对累计全文跑一遍 8+ 正则清洗（O(n²)），推流线程却只按 0.3s 采样**<br>把 _clean 挪进 _pusher（推送前清洗一次），主循环只存原始累计文本（或只做 join）；定稿帧路径不变。 | 每条流式回答的清洗次数从 ~帧数（数百次）降到 ~推送次数（每 0.3s 一次，数十次），长答案下每条省掉数百次全文正则扫描；SAE 单实例（--workers 1）并发多路流式时 CPU 峰值明显下降。 | S |
| 34 | `opensearch_pipeline/llm_generator.py:465` | **/api/ask 主链路串行外部调用 + 120s LLM 超时，线程池（默认 40）易在慢 LLM 突发下饱和**<br>单次 /api/ask 在一个线程池线程内串行：get_query_embedding（HTTP request_timeout=30）→ HA3 query（timeout=30）→ 可选 rerank → LLM requests.post（timeout=120，465/585行）。所有 sync def 端点共享 Starlette/AnyIO 默认 ~40 线程池。LLM 120s 超时偏长：一批慢 LLM 调用即可占满 40 线程，令后续所有 sync 端 | 避免少量慢/挂起 LLM 请求拖垮整实例吞吐；在 --workers 1 下把并发上限与超时对齐，降低突发下的排队延迟。 | S |

## E. 批量化 / 消除 N+1（摄取 & 落库）

| # | 位置 | 优化 | 预期收益 | 工作量 |
|---|------|------|----------|--------|
| 35 | `console-app/src/composables/useKb.ts:544` | **批量上传严格串行：每个文件 upload-url → OSS PUT → register 首尾相接**<br>uploadBatch 的 `for (let i = 0; i < files.length; i++) { ... await apiJson(upload-url) ... await putWithProgress ... await apiJson(register) }` 完全串行。改为并发度 2-3 的小并发池（QueueRow 已按行独立展示进度，UI 无需改）；register 若后端有顺序敏感可保持串行、仅并行 PUT 阶段。 | N 个大文件批量上传的墙钟时间约 ÷2-3（瓶颈是 OSS PUT 带宽往返，25min 超时的大文件场景收益最明显）。 | M |
| 36 | `opensearch_pipeline/allowed_depts_reconcile.py:78` | **allowed_depts_reconcile 逐文档 4 次 SQL 往返（N+1）**<br>保持单一注入点写路径不动，前置批量预计算：一条 GROUP BY doc_id 的查询拉全部 approved grants、一条拉全部现存投影与版本/锁状态，先在内存 diff 出真正漂移的 doc 子集，只对该子集调 materialize_doc_allowed_depts 复核+写。 | 无漂移的常态轮次从 ~4×N 次往返降为 2 条聚合查询；授权规模增长后 stage-3 pre-drain 延迟保持常数。当前 N 小、属前瞻项。 | M |
| 37 | `opensearch_pipeline/pipeline_nodes.py:1561` | **classify 认领预占逐行 UPDATE：一批 100 行 = 100 次语句往返**<br>两步式：单条 UPDATE … WHERE (doc_id,version_no) IN (…) AND content_process_status IN (…)，随后 SELECT 这些键中 status='PROCESSING' 的行构建 valid_canonicals（或沿用 stage-2 loader 的 FOR UPDATE SKIP LOCKED 单步认领模式）。 | 100 次往返降为 2 次；RDS 跨云 RTT 下每批省约 98×RTT（次要收益，代码推导）。 | M |
| 38 | `opensearch_pipeline/pipeline_nodes.py:6419` | **node_update_index_status 逐 chunk UPDATE 改批量 upsert**<br>按 500-1000 行分批 `INSERT ... ON DUPLICATE KEY UPDATE`（uk_chunk_id 唯一键已在）或 CASE WHEN 批量 UPDATE，提交点不动。 | N chunk 的往返从 N 次降到 N/500 次：18k-chunk 全量重建的状态回写从 ~18k 次串行往返（数十秒-分钟级）降到 ~36 次；同时缩短长事务锁持有窗口。 | M |
| 39 | `opensearch_pipeline/retriever.py:396` | **_deny_revoked_cross_dept 每请求一次权威表 RDS 往返（Phase D 已双端 ON）**<br>每个含跨部门命中的查询都 _get_db_conn() + resolve_allowed_depts 查 kb_access_request（retriever.py:396-404）。可加 30-60s TTL 的 (doc_id → approved 组码) 进程内缓存（--workers 1 单进程，天然一致），revoke 端点主动失效即可保留"撤销即时生效"语义。 | 跨部门授权文档流量下每请求省一次 RDS 往返（池化后主要是查询 RTT，约 1-5ms + 锁定一个池连接）；随授权数增长收益线性放大。注意保守设 TTL——该函数是机密性 fail-closed 兜底。 | M |
| 40 | `opensearch_pipeline/api.py:3749` | **kb_my_access_requests 逐行派生同步态：最多 100 行 × 每行 2 条 SQL 的 N+1**<br>先收集全部 (doc_id, version_no)，一条 `GROUP BY doc_id, version_no` 拿计数，再一条 `WHERE (doc_id,version_no) IN (...)` 拿 allowed_depts 并在 Python 端并集，循环内纯内存判定。 | 该端点 SQL round-trip 从 O(行数×2) 降到常数 3 条；申请人列表页（每次进「我的申请」都触发）延迟显著收敛。 | S |
| 41 | `opensearch_pipeline/api.py:674` | **/api/ask 同步落库阻塞响应：qa_session_log INSERT（开会话历史时 +1 次 upsert commit）在返回用户之前串行执行**<br>用 FastAPI BackgroundTasks（或单线程队列）把 log_qa_session 移出响应关键路径；qa_logger 本就吞异常、无返回值依赖（message_id 已预生成），零契约变化。 | P50 减 1-2 次 RDS 往返（同区约 5-30ms）；更重要的是切断尾延迟耦合——RDS 慢查询/故障时回答不再跟着变慢（当前 slow-INSERT 直接加到用户等待上）。 | S |
| 42 | `opensearch_pipeline/pipeline_nodes.py:2038` | **node_detect_sensitive 敏感命中逐条 INSERT + 每篇独立连接**<br>hits 组装参数列表后 cursor.executemany 一次插入；连接可与本节点循环共享（每篇一取一还已足够，无需更细）。 | N 条命中的插入往返 N→1；实现即 pymysql 原生多值 INSERT，收益与命中数成正比。 | S |
| 43 | `opensearch_pipeline/pipeline_nodes.py:4926` | **chunk_meta 插入：单次 executemany 携带全批 full chunk_text，应在同一事务内分批，绑定包大小与锁持有时间**<br>`cursor.executemany(insert_sql, insert_rows)`（4926）一次性提交全批 27 列行（含完整 chunk_text 与 extra_json，单行可至数 KB）。pymysql 会把它改写为一条巨型多行 INSERT。改为同一事务内每 500-1000 行一次 executemany（commit 位置不变，原子性不变）。 | 消除大批次撑爆 max_allowed_packet 的失败模式（健壮性收益为主），锁持有更平滑；速度不回退（多行 INSERT 批大小 500-1000 已在收益平台区）。依据：4893-4926 insert_rows 无上限累积 + 单次 executemany。 | S |
| 44 | `opensearch_pipeline/pipeline_nodes.py:6419` | **node_update_index_status 逐 chunk 单条 UPDATE（每批最多 1000 次 RDS 往返）**<br>成功路径的 12 个 SET 字段除时间戳外全批相同（同 model/version/dim/index_name/job_id、err=NULL），可合并为一条 `UPDATE chunk_meta SET ... WHERE chunk_id IN (...)`；仅失败 chunk 保留逐条（写 error 详情）。bulk_job 那层同理可 executemany。 | 每 1000-chunk 批从 ~1000 次往返降到 ~2 次；按 RDS 单次 1-3ms 计每批省 1-3s，整库排空省分钟级，且缩短 04 事务持锁时间。 | S |
| 45 | `opensearch_pipeline/pipeline_nodes.py:5413` | **node_deactivate_old_chunks 按 doc 三重 N+1（SELECT 旧 id / HA3 删除请求 / RDS UPDATE）**<br>旧 id 用一条 OR/(doc_id,version_no) 元组查询取回并按 doc 分组；HA3 删除跨 doc 合并成 100 条/请求的共享批（与 F3 修复合并做）；RDS 停用 UPDATE 同样合并。 | 批内 N 个版本更新 doc 的往返从 3N 降到 ~3；N 大时（重灌/迁移日）省秒级到分钟级，并减少 HA3 请求数。 | S |
| 46 | `opensearch_pipeline/pipeline_nodes.py:5133` | **node_acquire_index_lock 每 (doc,ver) 最多 3 条串行 UPDATE**<br>主认领改为一条带 (doc_id,version_no) 元组 IN 的集合式 UPDATE，再对 rowcount 差集走 SUCCESS-relock/stale-takeover 两条兜底（保留现有 changed-rows 语义与 updated_at=NOW() 刷新）。 | 锁获取从百级往返降到 ≤3 条语句，缩短认领事务持锁窗口（降低与 serving 侧 ACL 标脏写入的锁冲突概率）。 | S |
| 47 | `opensearch_pipeline/qa_logger.py:238` | **qa_logger 开会话历史后每条消息两次独立 commit（主 INSERT + qa_conversation upsert）**<br>把 upsert 并入主事务（同一 commit），失败时降级为现状两段式；或 upsert 改 fire-and-forget 到后台队列。RAG_CONVERSATION_HISTORY 目前生产默认 OFF，属预防性优化。 | flag 开启后每条问答少 1 次 RDS 往返+fsync（~5-15ms）；量级小，但叠加在响应关键路径（见 perf#1）上。 | S |

## F. 并发化串行 I/O（摄取 / 对账）

| # | 位置 | 优化 | 预期收益 | 工作量 |
|---|------|------|----------|--------|
| 48 | `Makefile:71` | **1,627 个测试全程串行（本地与 CI 均无并行）**<br>引入 pytest-xdist：`-n auto --dist loadfile`。loadfile 保证同文件测试落在同 worker，天然兼容现有的模块级 autouse fixture（test_pipeline 的 DB wipe、import 期 env setdefault）；需先把共享本地 DB 的文件（test_pipeline / test_concurrency / test_classification / test_image_funnel 的 DB 用例）归入同一 xdist group 或标 ser | 套件以 CPU 密集的纯单元测试为主（130 文件 / 1,627 用例，grep 统计），墙钟时间大致随可用核数下降；CI ubuntu runner 4 vCPU 与本地 Apple Silicon 均能吃到并行收益。 | M |
| 49 | `opensearch_pipeline/pipeline_nodes.py:4679` | **node_publish_to_rag_ready：每文档 2 次串行 OSS put + 1 次独立连接/commit，可复用连接并并行上传**<br>循环内 `bucket.put_object(rag_ready_key, ...)` + `bucket.put_object(metadata_key, ...)`（4679-4682）串行执行，随后每文档 `conn = _get_db_conn(...)` → UPDATE → commit → close（4692-4715，SKIPPED_EMPTY 分支 4613-4640 同样）。改为：整个发布循环持有一条池化连接（每文档 | 发布节点墙钟约按并发度线性缩短（200 串行 RTT → ~200/8），RDS 往返减少 ~100 次连接检出。依据：4679-4715 逐文档串行结构 + 认领批上限 100。 | M |
| 50 | `opensearch_pipeline/pipeline_nodes.py:5999` | **HA3 pushDocuments 子批并行化（2-4 路）**<br>ThreadPoolExecutor 2-4 worker 并行推子批（cmd:add 按 rds_id upsert 幂等、批间无顺序依赖；结果聚合后再走既有 verify/回写）。 | 推送墙钟时间近似按并行度线性下降（180 次串行往返 → ~45-90 次墙钟当量）；不触碰 DAG3 的“先索引后 deactivate”顺序不变量（deactivate 在整个 push+verify 之后）。 | M |
| 51 | `opensearch_pipeline/reconcile.py:176` | **HA3 PK 全量扫描逐桶串行 + 每个非空桶至少 2 次零向量 kNN 查询——并行化桶扫描**<br>(1) 用 4-8 线程并发扫桶（每线程独立 HA3 client，查询纯 I/O 密集），rows dict 加锁或按桶分片后合并；(2) 两份实现合并为一个共享 enumerate，spot-check 场景内一次扫描喂 parity 与 orphan 两个消费者。 | 站桩健康作业（02:30 cron + 每次 spot-check）的 HA3 扫描墙钟时间约降为 1/4~1/8（查询数不变、并发摊薄 RTT）；合并实现后同进程重复全扫减半。依据：扫描成本 = 桶数×轮数×单查 RTT，纯串行无数据依赖。 | M |
| 52 | `opensearch_pipeline/retriever.py:1503` | **multi_query_mode=auto 时分解 LLM 调用与检索完全串行**<br>maybe_decompose（decompose_timeout=8s，config.py:346）在任何检索开始前串行执行（retriever.py:1503-1510），而启发式 looks_multi_intent 极易触发（含"、/和/？？"即中，query_decomposer.py:31-37）。可在发起分解的同时并行启动原查询主路检索，分解返回后仅补跑子查询路，交错合并逻辑不变。 | 开启该功能时（当前 dark），多意图问题的首字延迟减去几乎整个 LLM 判别耗时（典型 1-3s，最坏 8s）；未触发分解的问题零回归。 | M |
| 53 | `opensearch_pipeline/retriever.py:1529` | **cosurface 的 HA3 补图查询可与 stitch/expand 并行**<br>cosurface_doc_images 排在 stitch（1 次 RDS）+ expand（最多 3 次 RDS）之后串行执行（retriever.py:1523-1530），但它只需要 top 文档的 doc_id 集合与"结果中是否已有 image chunk"——两者在 doc-cap 选定 top_k 后即已确定。可用一个 future 在 stitch 开始时并发发起 HA3 图片查询，expand 完成后合并插入。 | 图文路径（控制台流式）每请求从关键路径上摘掉一次 HA3 kNN 往返（数十 ms 量级）；实现需保证 doc_id 集在 expand 后不变（expand 不改 doc_id，成立）。 | M |
| 54 | `opensearch_pipeline/dataworks_orchestrator.py:243` | **Stage-2 loader 串行逐文档拉 OSS canonical**<br>认领提交后用 ThreadPoolExecutor(8) 并行 get_object（认领事务已提交，读操作无锁依赖；失败回写逻辑保持逐条）。 | 每批加载时间 ≈ 除以并发度（100 × 50-200ms 串行 = 5-20s → ~1-3s）；全量重灌场景收益线性放大。 | S |
| 55 | `opensearch_pipeline/dingtalk_identity.py:366` | **多部门用户首解析时 _fetch_dept_name 串行逐部门 HTTP（每次 5s 超时），阻塞检索**<br>L366-370 `for did in dept_id_list: nm = _fetch_dept_name(token, did)` 串行发 N 次钉钉 department/get，每次 timeout=5s。改用小 ThreadPool 并发拉取部门名（结果集合仍去重）。 | cache-miss 首问延迟从 N×RTT(最坏 N×5s)降到 ~1×RTT。该解析在 dingtalk_bot.py:530 位于 retrieve_and_enrich 之前，直接压在用户首问关键路径上；跨部门用户(如产线/贸易多部门)收益最明显。 | S |
| 56 | `opensearch_pipeline/pipeline_nodes.py:6687` | **04b parity：固定 30s settle 睡眠 × 多轮 + 逐 PK 串行 point-read**<br>settle 改为轮询早退（每 3-5s 抽查一个刚推 PK，present 即继续），point-read 用小线程池并行（仿 RAG_VLM_CONCURRENCY 的既有模式，QueryRequest 相互独立）。 | 多数批实际几秒即可见，早退可把每批 30-90s 等待压到 <10s；18 批排空省 5-20 分钟。 | S |
| 57 | `opensearch_pipeline/reconcile.py:445` | **run_raw_parity_check 对每篇 current-version active 文档串行发一次 OSS HEAD**<br>复用 CS4 的 list-then-diff 模式：先按 raw/ 前缀分页 ListObjects 一次拉全 key 集合，raw 集合差集为空即完成；仅对差集候选（通常个位数）逐个 HEAD 确认（防前缀外 key 误报，与 CS4 的 verify_fn 语义一致）。 | N 次串行 HEAD → ~N/1000 次 LIST + 少量 HEAD；raw 对账从 O(文档数×RTT) 变成近常数，与 CS4 代码路径统一。 | S |
| 58 | `opensearch_pipeline/reconcile.py:362` | **run_oss_parity_check 的差集候选串行 HEAD 验证**<br>候选 >50 时切换为对候选 key 的公共前缀做 ListObjects 建 set 后 diff，或 ThreadPool 并发 HEAD。 | 异常放大场景下 CS4 从分钟级回到秒级；常态影响小，属健壮性顺带优化。 | S |
| 59 | `opensearch_pipeline/retriever.py:96` | **查询侧 embedding 超时/重试预算过大：串行最坏 ~93 秒才返回 500**<br>get_query_embedding 硬编码 request_timeout=30（retriever.py:96），max_retries 取 config.embedding.max_retries=2（config.py:273）→ 3 次尝试 × 30s + 退避 1s+2s ≈ 93s 最坏串行阻塞，且这是每个 /api/ask{,/stream} 的第一步（retriever.py:1500）。查询侧改为 timeout≈8s、retries=1（入库侧保留 30s/2 不动——em | 把 DashScope 挂起时的单请求尾延迟从 ~93s 压到 ~17s；SAE 单实例 --workers 1 下同时减少线程池被挂起请求占满导致全端点停摆的窗口。 | S |
| 60 | `opensearch_pipeline/retriever.py:781` | **stitch 与 expand 各自 checkout 连接、共 4 次串行 RDS 查询，可合并**<br>stitch_neighbor_chunks（retriever.py:781-802，1 次查询）与 expand_step_context（retriever.py:929-1005，最多 3 次查询：meta/siblings/vk_refs）各自从池里取连接、串行跑。传入共享连接或把两阶段的 chunk_meta 读合并为一轮（meta+邻居可 UNION 或一次 IN 查询取回后内存分桶）。 | 每请求省 1 次池 checkout + 1-2 次查询 RTT（毫秒级/请求，但作用于每一条问答）；同时减少 --workers 1 下池连接的并发占用。 | S |
| 61 | `opensearch_pipeline/spot_checker.py:400` | **spot-check 安全复审逐文档串行调 LLM（单次 timeout 90s）**<br>沿用 image_funnel 的并发模式（如 ThreadPoolExecutor(max_workers=4-8)）并发 LLM 审查；quarantine 写路径保持串行/加锁即可（命中率极低）。 | 抽检墙钟 ≈ 除以并发度；也降低单篇 LLM 慢响应拖垮整轮的概率。依据：循环体之间无数据依赖，仅 report 聚合需收敛。 | S |

## G. 抽取 / 多模态重复解析

| # | 位置 | 优化 | 预期收益 | 工作量 |
|---|------|------|----------|--------|
| 62 | `opensearch_pipeline/extraction/unified_extractor.py:625` | **同一 xlsx 被完整解析最多 3 次：probe load、正式 load、图片提取 load**<br>625-631 的 probe（read_only 探 max_row）结果与 631 的正式 wb 可以合一（先 read_only 探，若决定非 read_only 才二次 load）；再把已加载的 wb 传给 extract_images_from_xlsx（新增可选参数，兼容独立调用），避免 image_extraction_utils.py:424 第三次 zip 解包。 | 图文型 xlsx 注释自述可达数十 MB（621 行："文件可达数十 MB"）——省掉 1-2 次全量 OOXML 解析，每文档省数秒 CPU/IO；对回灌批次线性放大。 | M |
| 63 | `opensearch_pipeline/extraction/unified_extractor.py:498` | **每个 DOCX 被完整解析两次：extract_docx_with_images 与 extract_images_from_docx 各自 docx.Document(local_path)**<br>492 行已拿到解析好的 document（在 docx_extractor 内部）；给 extract_images_from_docx 增加可选 document 参数，_extract_docx 串起来复用（或让 extract_docx_with_images 顺带返回 document）。 | 省一次完整 OOXML 解包+lxml 树构建；对图多的 SOP docx（数 MB、几十图）每文档省数百 ms~秒级，批量回灌线性收益。 | M |
| 64 | `opensearch_pipeline/extraction/ocr_client.py:273` | **OCR 整页走 2x 渲染 PNG 上传，payload 远大于 vlm_rebuilder 已验证的 JPEG 方案**<br>`img_data = pix.tobytes("png")` 直接 base64 上传；vlm_rebuilder.py:79-87 已因"首版踩到 write timeout"改为 JPEG q78 + 限边 1568px。ocr_client 复用同一压缩（或直接调 _render_page_image 的压缩段）。 | 2x 渲染的 A4 中文页 PNG 常 1-3MB，JPEG q78 约 200-400KB → base64 体积降 ~5x，逐页 OCR 上传时间与 write-timeout 概率同步下降（vlm_retry.py 模块 docstring 记载的 381KB 即已触发过上传超时）。 | S |
| 65 | `opensearch_pipeline/extraction/unified_extractor.py:1740` | **_pages_needing_ocr 每个 PDF 计算两遍（_needs_ocr 一遍、_apply_ocr_fallback 再一遍），page_count<=0 时的库回退探测也跑两遍**<br>1459 行 `if self._needs_ocr(result):` 内部已算出 needy 列表，1740 行又重算；改为 `needy = self._pages_needing_ocr(result); if needy: result = self._apply_ocr_fallback(task, result, needy)`。 | 正常路径省一次全 blocks 逐页聚合（小）；page_count<=0 的坏 PDF 路径省一次 pypdf/pdfplumber 重开探测（1676-1695 行，可达秒级）。纯删重复，无行为变化。 | S |
| 66 | `opensearch_pipeline/extraction/vlm_rebuilder.py:70` | **vlm_rebuilder._render_page_image 每页 fitz.open 重开整个 PDF**<br>maybe_rebuild_pdf（201 行循环）与 maybe_refine_tables（391 行）在循环外 open 一次 doc，循环内只 load_page+render，finally 统一 close；_render_page_image 增加接受已开 doc 的参数。 | 20 页扫描件升级 = 20 次重复 open/parse PDF 结构；大 PDF（数十 MB）每次 open 数百 ms，合计省数秒/文档。开关默认 OFF，但这是启用前应还的账。 | S |
| 67 | `opensearch_pipeline/pipeline_nodes.py:845` | **canonical JSON 以 indent=2 序列化上传，stage-2 再整包下载解析——块级 OCR/VLM 文本被缩进空白放大**<br>改 json.dumps(canonical, ensure_ascii=False, separators=(",", ":"))；如需人读可另存 .md（已有）。 | OSS 存储与两次传输（写+读）体积按缩进空白占比下降（块级 JSON 通常两到四成为缩进/换行，具体取决于文档），序列化 CPU 同步减少；一行改动零风险。 | S |

## H. 前端（控制台）渲染 / 拉取

| # | 位置 | 优化 | 预期收益 | 工作量 |
|---|------|------|----------|--------|
| 68 | `console-app/src/composables/useAsk.ts:180` | **流式吐字泵每 tick 全量 stripImg + renderMd + 整块 innerHTML 替换：O(n²) 渲染**<br>pumpTick 每 ~24ms 执行 `ch.setHtml(ai, renderMd(ch.text(ai).slice(0, next)))`，其中 ANSWER_CH.text=stripImg(ai.raw) 是全文正则扫描，renderMd 全文重解析，v-html 又整块替换 DOM 触发全段重排。可：按 raw 长度缓存 stripImg 结果；renderMd 只重渲最后一个未闭合段落、前缀段落 HTML 缓存复用；或定稿前只用轻量段落渲染 | 长答案（数千字、30s+ 流）从每 tick O(全文) 降为 O(尾段)，办公低配机上流式期 CPU/重排显著下降（该路径是每帧热点，answer 与 reasoning 两通道同款）。 | M |
| 69 | `console-app/src/composables/useAsk.ts:540` | **persist 的 deep watch 在流式期间每次 chunk 变更都全量深遍历所有会话**<br>`watch([conversations, activeId], schedulePersist, { deep: true })` 依赖收集覆盖全部会话（含 30 条持久化历史的所有消息字段），流式期间 ai.raw/html 每 tick 变更都触发一次全树 traverse（写盘虽有 400ms 防抖，遍历本身不防）。改为在既有的明确变更点（ask/onEvent 收尾/removeConversation/switchTo 等）手动 schedulePersist()，或 w | 去掉流式期每帧 O(全部历史消息) 的响应式遍历，与上一条叠加是打字卡顿的两大来源。 | S |
| 70 | `console-app/src/composables/useAsk.ts:402` | **侧栏会话列表 computed 在流式期间随每次消息变更重算排序/全文搜索**<br>Sidebar.vue:28 `computed(() => searchConversations(q.value))` 依赖 conversations 内容；searchConversations 每次复制+排序全部会话，q 非空时还对每条消息 (m.text\|\|m.raw\|\|m.answer).toLowerCase() 全文扫描——流式 raw 每帧变化即重算。可对搜索输入防抖 + 排序键只依赖 updatedAt/title 的浅 computed，搜索命中缓存 lowercase。 | 带搜索词挂着侧栏时流式期从每帧 O(全部消息文本) 降为防抖后偶发一次。 | S |
| 71 | `console-app/src/views/QaView.vue:32` | **QaView 对 messages 的 deep watch 驱动滚动跟随：同样在每个流式 tick 全量深遍历**<br>`watch(messages, () => { if (atBottom.value) nextTick(...) }, { deep: true })` 只为滚动跟随却深订阅整个活动会话。改 watch `() => { const l = messages.value[messages.value.length-1]; return (l?.html?.length \|\| 0) + ':' + messages.value.length }` 这类窄信号，或 | 长会话（几十条消息）流式期减少每帧遍历；与 #2 同类，改动极小。 | S |
| 72 | `schema/007_kb_etag_dedup_index.sql:20` | **007 idx_etag 尚未应用（文件自述 gated）—— 自助上传注册后的跨部门内容查重 WHERE v.etag=%s 全表扫**<br>按 003/004/005 同款 information_schema 守卫脚本把 `CREATE INDEX idx_etag ON document_version (etag)` 应用到生产（非唯一索引，文件已论证不能 UNIQUE 的理由）。 | 上传热路径上的 advisory 查重从全表扫降为索引点查；随 document_version 行数增长收益放大。风险为零（纯加索引、fail-open 查询）。 | S |

## I. CPU 微优化（正则预编译 / O(n²) 估算）

| # | 位置 | 优化 | 预期收益 | 工作量 |
|---|------|------|----------|--------|
| 73 | `opensearch_pipeline/auth_token.py:53` | **auth_token._get_signing_key 每次签发/校验都重读环境变量并重新 encode 密钥（每个带 Bearer 的请求热路径各一次）**<br>首次成功解析后缓存 key bytes 于模块级（环境变量进程内不变；ephemeral 分支本就已缓存 _ephemeral_key），后续直接返回缓存。verify_session_token（L150）与 issue_session_token（L140）各调用一次。 | 微小但真实：省去每请求一次 os.environ 查找 + str.encode（微秒级）。诚实标注：这是打磨级收益，不影响吞吐瓶颈，仅在做同类整理时顺手收敛。 | S |
| 74 | `opensearch_pipeline/chunker.py:1645` | **_CLAUSE_RE 每次调用 _chunk_by_clause 都重新 re.compile**<br>提升为模块级或类属性常量（同文件 _STEP_BOUNDARY_RE 已是类属性，照此办理）。 | 省每文档一次正则编译（微秒~毫秒级/doc）；批量 re-chunk 数千文档时的常数削减，量级小但零风险。 | S |
| 75 | `opensearch_pipeline/chunker.py:1316` | **procedure_parent token 预算循环 O(n²)：每加一个标题就对全量累计文本重跑 _estimate_tokens 正则**<br>增量维护：预计算 base tokens，逐条累加单个标题的 token 估算（_estimate_tokens 对拼接近似可加），O(n²)→O(n)。 | 长 SOP 文档毫秒级节省；纯 CPU 微优化，依据=循环结构本身。建议顺手做，不值单独排期。 | S |
| 76 | `opensearch_pipeline/chunker.py:2135` | **_chunk_default 每挂一张图就对整个 chunk_text 重跑 _estimate_tokens**<br>同一 chunk 的多图后缀先收集、一次性拼接后重算 token_count（表格路径 L2185-2188 已是此模式，内联路径对齐即可）。 | 每 chunk 图片数上限 3（row_card 8），节省为常数级——收益微小，仅作代码一致性顺手项。 | S |
| 77 | `opensearch_pipeline/llm_generator.py:383` | **会话历史只按轮数裁剪、无字符预算 —— 多轮长答案线性放大每轮 prompt token 成本**<br>给历史加字符预算（如从新到旧累计 ≤8-12k 字符即截断，或对更早轮只保留 user 问题+答案首 300 字）；入口在 _build_messages 或 _prepare_ask 单点。 | 长会话每请求 prompt token 上限从 ~数万降到可控常数（DashScope 按 token 计费，多轮重度用户成本可降数倍）；同时缩短 LLM TTFT。收益与会话长度成正比，短会话无影响。 | S |

## J. CI / 测试

| # | 位置 | 优化 | 预期收益 | 工作量 |
|---|------|------|----------|--------|
| 78 | `tests/test_pipeline.py:23` | **reset_db_state 对 test_pipeline.py 全部 31 个测试无差别执行 3 条真实 DML（含纯结构断言测试）**<br>把 reset（:23-26 的 UPDATE document_version / DELETE chunk_meta / DELETE document_sensitive_finding）从模块级 autouse 收窄到真正读写 DB 的用例，或在 cfg.simulate_db=True 时直接跳过 reset 逻辑本身（与 finding-1 同一处修改）。 | 本地栈在线时每轮 make test 省去 ~90 次（31 测试 × 3 语句）不必要的 MySQL 往返；更重要的副产品是解除「结构测试被 DB 可用性劫持」的耦合（finding-1）。 | S |
| 79 | `tests/test_pipeline.py:311` | **版本更新/多文档场景类每个测试都从头重跑 DAG1+DAG2**<br>TestVersionUpdateScenario 三个测试（:298-333）各自 `build_dag1_raw_to_canonical().run(ctx)` + `build_dag2_canonical_to_chunk().run(ctx)` 重建同一前置状态；可用 class 级 fixture 生成一次 DAG1+DAG2 后的 ctx 深拷贝分发（simulate 模式 ctx 为纯 dict，可 copy.deepcopy 隔离）。 | 省去重复的抽取/分类/切分模拟计算。收益中等（simulate 模式单轮很快），排在前三项之后；若后续场景测试继续增多则收益放大。 | S |

## K. 其它

| # | 位置 | 优化 | 预期收益 | 工作量 |
|---|------|------|----------|--------|
| 80 | `opensearch_pipeline/api.py:1329` | **hot-questions / rephrase 问题池对 query_text 做非 sargable 全表聚合**<br>hot_questions（1323-1335）与 _success_question_pool（1393-1405）对 qa_session_log 做 `GROUP BY query_text` 且过滤 `CHAR_LENGTH(query_text) BETWEEN ...`（函数条件不可走索引），近 30 天窗口 → 冷缓存时全扫。虽有 1h 进程内缓存，但缓存过期的首个请求发生在 chat 首页热路径。建议改为离线/定时物化（qa_daily_metric | 消除每小时一次的 qa_session_log 全表聚合冷启动尖峰，降低首屏 P99。 | M |
| 81 | `opensearch_pipeline/reranker.py:134` | **rerank_route_vl=True 下单张图片污染整池：20 条候选全走 VL 重排**<br>use_images = bool(multimodal) or (bool(cfg.rerank_route_vl) and any(_img_key(c) for c in chunks))——纯文本查询的 20 条池（rerank_pool=20，config.py:224）里只要混进 1 条带图 chunk，整池路由到 qwen3-vl-rerank（更慢更贵）。可按图片占比/意图设阈值（如 ≥3 张或查询含视觉词才走 VL），或对 VL 池只给带图 doc 附 image_url | 文本主导流量下降低重排延迟与单价（VL 对 20 条 1200 字符文档的推理开销高于文本 rerank）。注意当前路由是 A/B 数据驱动选择（rerank_findings.md），改动需重跑 251 题金集验证不回退 recall@1。 | M |
| 82 | `console-app/src/views/ManageView.vue:59` | **管理台重复拉取：App ready 已预载的审批/授权队列在每次进 /manage 时无条件重拉，approve/reject 后又全量双拉**<br>App.vue:32 ready 时已 loadApprovals+loadAccessRequests（为侧栏红点）；ManageView onMounted（52-62 行）再次全部发起，且 approve()/reject() 每单都 `await loadApprovals(); await loadDocs()`（useKb.ts:574）串行全量刷新。可加简单 staleness 门（如 30s 内不重拉）+ approve 成 | 管理员最常用页面每次进入省 2 个冗余往返；连续审批 N 单从 2N 次全量列表拉取降为 N 次本地更新+少量刷新。 | S |
| 83 | `opensearch_pipeline/api.py:3648` | **kb_audit_log 只有 idx_doc_id：审批历史两条 action_type 过滤 + created_at 排序查询全表扫 + filesort**<br>加复合索引 (action_type, created_at)（gated DDL，additive）。 | 两条历史查询从全表扫+filesort 变为索引倒序取前 200；随审计表日增，退化曲线被拉平。 | S |
| 84 | `opensearch_pipeline/api.py:4162` | **_reconcile_contributions_searchable 在 4 个 GET 端点上无条件写-on-read**<br>先 `SELECT EXISTS(... WHERE ingestion_status='registered' LIMIT 1)` 短路，或加进程内 60s 节流（单 worker 安全），仅在存在待对账行时才跑 UPDATE。 | 四个高频读端点在稳态（无待入库贡献）下少 2 条 UPDATE + 1 次 commit/请求；改动几行。 | S |
| 85 | `opensearch_pipeline/chunker.py:1270` | ✅ **消除前导文本双发（与 P1 finding 同源）——每个 step 文档少付一份 embedding 与 RDS/HA3 写入**<br>删除其中一个发块循环。 | 每个带前言的 step 路由文档减少 1~数个 chunk 的 embedding API 调用与索引写入；这是本文件唯一有真实外部成本（API 计费/索引体积）的项，收益依据=重复块机制本身（已实测复现）。 | S |
| 86 | `opensearch_pipeline/chunker.py:1086` | **Phase 3.5 图片 caption 反查用嵌套线性扫描**<br>构建时直接携带 caption/ocr（img_ref_entry 里已有 visual_summary/ocr_text 两键，内层循环可整体删除），或预建 image_index→entry dict。 | k（单步图片数）通常 ≤8，纯代码简化；顺带消除 image_index 为 None 时反查首个 None 项的潜在错配面。 | S |
| 87 | `opensearch_pipeline/dingtalk_bot.py:874` | **点赞/踩回调为一次反馈打开 2 个独立 DB 连接**<br>一次 upvote 回调先在 _card_callback_authorized(L874) 用 _get_db_conn() 查一次 user_id 做归属校验，随后 handle_feedback→_save_feedback(feedback_handler.py:125) 再开一个新连接查 qa_session_log 上下文并 INSERT。可把归属校验的 SELECT 与 _save_feedback 的 SELECT 合并为一次查询/一个连接（归属列 user_id 本就在同表同行）。 | 每次反馈回调少 1 次连接获取+SELECT；在无连接池复用时省一次 TCP+握手，降低回调尾延迟与 RDS 连接压力。 | S |
| 88 | `opensearch_pipeline/extraction/image_extraction_utils.py:553` | **_find_asset_by_md5 对每个待配对图片全量重读+重哈希所有已导出资产文件（O(pics×assets) 磁盘 IO）**<br>导出循环里已经算过每个 blob 的 MD5（456 行）——把它存到 ImageAsset（如 asset.md5），_enrich_xlsx_annotations 直接建 {md5: [assets]} 字典查找，删除逐文件 open+hashlib.md5(f.read())。 | 60 图的图文作业指导书、方式A+方式B 各触发一次配对时，从 ~数千次文件读+哈希降到 0 次额外 IO；大图（数 MB 截图）场景 Stage-1 每文档省秒级。依据：556-567 行对 assets 逐个 `open(asset.local_path,'rb')` + md5，被两个匹配路径反复调用。 | S |
| 89 | `opensearch_pipeline/extraction/unified_extractor.py:1040` | **PPTX Phase-2 fallback 每张 slide 全量扫描累积 blocks（O(slides×blocks)）**<br>`slide_blocks = [b for b in blocks if b.page_num == page_num]` 只为取第一个 block 的前 50 字符；循环内记录本 slide 起始下标（进入 slide 时 len(blocks)），直接 blocks[start] 判断即可。 | 100+ 页培训 PPT 从 O(n²) 降 O(n)；单文档节省有限（毫秒~几十毫秒），属顺手清理。 | S |
| 90 | `opensearch_pipeline/ha3_reconcile.py:134` | **reconcile_ha3_orphan_pks 全表拉取 chunk_meta（含全部历史 inactive 行）但只用 active 行**<br>首次读加 WHERE is_active=1（与 L182 的二次确认读对齐），_classify_stale 的两个入参本来就只要 active 集合。 | 该对账的 RDS 读传输量降到 active 行占比（历史 churn 越久收益越大），一行 SQL 改动零语义风险——两处读取本就应同构。 | S |
| 91 | `opensearch_pipeline/oss_url.py:240` | **generate_signed_urls_batch 每个 key 重建 oss2.Auth/Bucket**<br>L240-241 循环对每个 oss_key 调 generate_signed_url，后者 L100-101 每次都新建 oss2.Auth + oss2.Bucket。批量场景（卡片重建 refresh_image_block_urls 重签多图）可构造一次 Bucket 复用，只循环 sign_url。 | 图文卡片重签 N 张图时从 N 次 Bucket 构造降到 1 次；单次开销小，量大时(多图卡)可测收益。 | S |
| 92 | `opensearch_pipeline/pipeline_nodes.py:843` | **node_build_canonical 每篇文档最多开 3 个池连接+3 次 commit，且每篇重复构造 OSS bucket 客户端**<br>bucket 提升到循环外构造一次；三段 DB 操作合并为每篇一个连接一个事务（skip 判定 SELECT + 状态 UPDATE 本就应同事务），或把最终的 canonical-keys UPDATE 跨文档攒批一次提交。 | 100 篇批次省约 200 次连接借还 + ~200 次 commit 往返（3→1 conn/篇，纯代码结构推导）；同时消除 skip 判定与写回间的窗口。 | S |
| 93 | `opensearch_pipeline/pipeline_nodes.py:4958` | **node_write_chunk_meta 状态闭环：每文档 3 次连接/提交 + O(D×C) 重扫，可合并为单连接单事务**<br>循环体内每个 (doc,version) 都执行 `doc_chunks = [c for c in valid_chunks if c.doc_id == doc_id and c.version_no == ver]`（4959，O(D×C) 全量重扫）+ `conn = _get_db_conn(select_db=True)` → 单条 UPDATE → commit → close（5076-5094），write_audit（5100）再各开一 | RDS 往返从 ~2×N 次 commit 降到 1-2 次，闭环阶段耗时随批量线性下降；顺带把「部分闭环成功部分失败」的中间态窗口收窄为单事务。依据：代码即每文档 conn+commit（5076/5087/5094），批量上限 100 来自认领 SQL LIMIT 100（dataworks_orchestrator.py:208-211）。 | S |
| 94 | `opensearch_pipeline/rate_limiter.py:333` | **rate_limiter._maybe_prune 在全局锁内对最多 2 万条目的字典做整体重建，且超阈值期间每个放行请求都触发一次**<br>记录 _last_prune 时间戳，把清理节流到例如 ≥30s 一次（或在锁外构建新字典后原子替换）。当前 _maybe_prune 在 admit_ask/admit_aux 的持锁路径上被每次调用（L297/L317），len(_minute)>20000 时执行 L336-339 的两个全量 dict comprehension；若扫描器维持 2 万+ 个 60 秒内活跃 key（恰是该阈值设计对抗的场景），清理一条都删不掉，退化为每请求  | 消除滥用高峰下每请求数毫秒级的持锁 O(n) 扫描——锁是所有 /api/ask 准入的串行点，节流后最坏情况从「每请求全量重建」降为「每 30s 一次」，对正常流量零影响（阈值以下路径不变）。 | S |
| 95 | `opensearch_pipeline/reconcile.py:326` | **_list_oss_keys 未设 max_keys，ObjectIterator 按默认 100/页分页**<br>oss2.ObjectIterator(bucket, prefix=prefix, max_keys=1000)（OSS ListObjects 单页上限）。 | LIST 调用数降为 1/10，CS4 job 的 OSS 阶段耗时同比例下降；一行改动。 | S |
| 96 | `schema/001_opensearch_pipeline.sql:258` | **kb_audit_log 只有 idx_doc_id —— console 审批历史两条查询按 operator_type/action_type 过滤 + created_at 排序，全表扫+filesort**<br>加复合索引 `(operator_type, action_type, created_at)`（或 `(action_type, created_at)`），使 api.py:3653 `WHERE a.operator_type='user' AND a.action_type IN ('APPROVE','REJECT') ORDER BY a.created_at | 两条历史查询从全表扫+filesort 变为索引区间倒序 LIMIT；随每日管线写入线性恶化的查询变为 O(limit)。 | S |
| 97 | `schema/007_kb_etag_dedup_index.sql:21` | **register 内容查重按 etag 查询，但 schema/007 的 idx_etag 标注 NOT YET APPLIED**<br>按 003/004/005 同款 information_schema 守卫脚本把 `CREATE INDEX idx_etag ON document_version (etag)` 应用到生产。 | 每次上传登记省一次 document_version 全扫；表规模目前不大，收益中等但成本几乎为零。 | S |

---

*本 backlog 由审查工作流的 `perf` 数组逐条导出，未增删。数字收益均标注为"依据从代码推导"，实施前应以 `qa_session_log.retrieval_latency_ms/llm_latency_ms` 等已落库指标做前后对比验证。*
