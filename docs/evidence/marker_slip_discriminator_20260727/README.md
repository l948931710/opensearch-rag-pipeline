# 判别实验产物（2026-07-27）

`marker_slip_discriminator_20260727.py` 那一轮的**原始 stdout**（`run_stdout.txt`）。

⚠️ 结构化的 `discriminate.json` 在一次 `git reset --hard` + 切分支 + 清理未跟踪文件的
操作中丢失（非本次分析所为）。**没有重跑补一份** —— 重跑会得到不同的 LLM 输出，
用新数字冒充旧结论比缺一个文件更糟。这里保留的是当时那轮的真实输出。

结论（判据先于数据写死，两种 marker 归属窗口互为对照）：

| 判定 | 条数 | 相似度差 a−b |
|---|---:|---|
| H-noimg（来源判对了、那篇没图） | **9** | +0.063 ~ **+0.283** |
| H-slip（数错号） | 2 | −0.038 / −0.032（a、b 绝对值均在 0.03~0.10 噪声区）|

两窗口一致率 **11/11**。被引文档类型：H-noimg 全落在 `procedure_parent`(5) /
`text_chunk`(4) —— 这两类天然不绑图，图挂在兄弟 `step_card` 上。

⇒ CiteFix 式语义重绑定是错方向；正解是逐文档粒度 + 弃权许可（#F-mm15）。
