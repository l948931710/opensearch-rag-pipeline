# HA3 485 盲文档治愈方案：恢复快照 + 全量重建（runbook + 工单素材）_DRAFT

> 2026-07-12。关联：`docs/audits/l7_refreeze_release_gate_2026-07-12.md`（法证）、
> `docs/ha3-doc-evaporation-incident-2026-07-06.md`（7-06 同签名先例）、
> memory `ha3-blind-485-incident-2026-07-12`。
> **执行门：Sam 逐步授权；工单先提（决定性问题见 §4），未回或未自行拍板前不动生产。**

## 1. 现状（2026-07-12 晚，全部实测）

- 485 docs / 10,366 chunks（7-06 16:00–7-07 12:00 写入窗）**检索盲**：hybrid 自查询 0/16。
- **doc store 完好（行级实证）**：点查 17/17 可达、perm/owner_dept 与 RDS 逐行一致、
  存储 dense 向量 17/17 完好（include_vector 读回，与重算 embedding cos≈1.0，L2=1.0）。
- **双臂分化**：BM25 倒排臂零写入下渐进自愈（5 token 探针 4/5 宿主召回、每 token 带回
  4–12 只盲 doc）；dense/HNSW 臂 5h+ 零恢复。weighted 0.7/0.3 下单臂行进不了 top-10
  → 现网用户仍失明。
- **全量重推已试且无效**：10:10–11:06 重推 485/485（RDS 全绿、双 active 清零），B2 仍
  0/15；单行治愈的探针 doc（chunk 74609）被批量重推再次吃掉——**批量窗口写复现触发条件**。
- 管理面 7 月零变更（变更历史实查）；客户端审计零删除/零 schema 操作 → 引擎内部问题。

## 2. 治愈主线：索引中恢复 FSM → 表编辑并重建（生产表有成功先例）

**6-28 先例（变更历史实录，全绿）**——staging 两次演练后动生产：

| 时间 | 步骤 | 关键 ID |
|---|---|---|
| 6-27 16:19→16:48 | staging 恢复+重建 | 快照 45077284 / 全量版本 1782549306 |
| 6-28 01:27→01:52 | staging 第二轮 | 快照 45121793 / 全量版本 1782582866 |
| **6-28 02:40→05:06** | **生产表 `fuling_kb_chunks`** | **快照 45128525 / buildTask 30343 / 全量版本 1782593755 / switch finished 05:06:24** |

先例证明：恢复快照在本表带得回全部数据 **含向量**（6-28 后 dense 正常服务、sparse 倒排
系该次全量物化）。恢复 FSM 节点序：初始化→创建快照→生成数据→检查生成状态→准备 BS 构建
数据→BS 构建检查→更新完成时间→终止构建任务→删除快照（生产轮全程 ~14 min，重建触发到
switch ~13 min，当时全表 ~27.6k 行）。

**与直接「手动触发全量」的区别（深度调研 3-0 实锤）**：API 推送源直接全量默认「从空数据
创建」= **清空存量、最多回追 swift 3 天** —— 绝对禁止。必须先跑恢复 FSM，再让重建以恢复
版本为数据源（6 月流程即此）。

## 3. 执行检查单（每步 Sam 亲手，逐步核对）

**前置（可先做）**
- [x] 索引变化率阈值开启（2026-07-12 已开，当前 20；**建议收紧到 5**——手动全量场景下
      正确重建变化率≈0，粗网眼会放过丢 15% 文档的坏全量）
- [ ] 报警组添加 `opensearch.ha3suez.searcher.index.rate_of_change`
- [ ] **冻结推送窗口**：暂停 DataWorks stage-1/2/3 日调度 + 不做任何手工重推
      （恢复→全量完成之间的新推送会丢；官方 3 天上限，按 6-28 先例实际 ~1-3h）
- [ ] 复检工具就位（零写可重跑）：`scratch/verify485_heal_20260712.py`（B2 抽样自查询）
      + 会话 scratchpad `paired_probe_485_20260712.py`（成对点查/自查询/BM25 扩样）
- [ ] 记录操作前基线：docCount / RDS 行数（当前 chunk_meta active 全量）/ B2=0/15

**执行（工单答复或 Sam 拍板后）**
1. [ ] 运维中心 →「从索引恢复数据」→ 等恢复 FSM 全绿（记录快照 indexId）
2. [ ] 表编辑并重建（无实质 schema 变更提交；确认数据来源=恢复版本，**不是**「从空数据创建」）
3. [ ] 盯变更节点到「构建索引 FULL」完成；若变化率>阈值暂停 → 核对新全量 docCount 与
       RDS active 行数一致后再手动强制切换；不一致 → 不切换，走回退
4. [ ] switch finished 后**立即**跑成对探针 + B2 抽样：
       - 成功判据：B2 ≥13/15 命中 top-10 且探针 chunk 74609 自查询可达
       - 同时点查抽 5 行确认字段/向量仍完好（防"治了检索丢了字段"）
5. [ ] +30min / +2h / +24h 三点延迟复验（7-06 教训：20 分钟稳定 ≠ 安全）
6. [ ] 全绿后：恢复 DataWorks 调度 → release-gate refreeze 链重启

**回退**：切换前发现异常 → 不强制切换，旧全量继续在线（恢复 FSM 本身不动线上索引，
先例中快照用毕即删）。切换后发现异常 → 无官方回滚语义（文档空白，工单问题 #4），
兜底 = 立即再走一轮恢复+重建（以坏全量为源则不可行——故 **切换前的 docCount 核对
+ 切换后立即自查询是仅有的两道闸**）。

## 4. 工单（治愈成败的决定性问题在此，先提）

**实例** `ha-cn-kgl4slr1n01`（杭州，向量检索版）·表 `fuling_kb_chunks`（swift/API 源，
autoBuildIndex:false，2 分片）

**必问**：
1. **「从索引恢复数据」快照的「生成数据」来源**：引擎 doc store 完整数据，还是从可检索
   索引枚举？——我们有 10,366 行"点查/字段/向量完好但 dense+BM25 可检索索引缺行"，
   恢复+全量能否把这批行完整带回新索引？
2. 7-06 16:00–7-07 12:00 高量写入窗（~40 min 1 万 chunk）后，实时段的 HNSW 索引项
   为何丢失？倒排臂为何能后台行级自恢复而 dense 臂不能（样本行 id=83138 宿主
   DOC_QUALITY_20260611201419_5A271A：BM25 已自恢复、44/44 chunk dense top-50 miss；
   id=76623 宿主 13092D BM25 仍缺）？是否与段合并/实时段持久化有关
   （同签名先例：6-22 96 行、7-06 64+69 行，docCount 与 RDS 一致但行不可检索/点查不达）？
3. 全量重推（cmd=add 同 PK 万级批量）为何无法补回 dense 索引项（今日 10:10–11:06 全量
   重推后自查询仍 0/15，且单行重推治愈的行被批量重推再次变盲）？安全的批量写入速率/
   节流参数建议？
4. 全量切换的原子性与失败回滚语义；「当前索引数据原地重建」模式的数据来源与适用性；
   `is_embedding_saved` 在本表的实际状态与含义（控制台不可见）。

**证据包**：`docs/ha3-doc-evaporation-incident-2026-07-06.md` + l7 法证 §release-gate +
三轮复检 JSON（10:10/11:06/16:26 B2 恒 0/15）+ 成对探针报告（点查 17/17 vs 自查询 0/16）
+ 向量完好性报告（17/17 cos≈1.0）+ 变更历史截图（7 月零管理面变更）+ 12:36/16:29
requestId（50c4325342201f5b2a13bc492473bf2c / 11fb6fe6bd7f577c4ceb031de2c58217 /
99f205b4155ee2bcf933f3c0cd85430d，供阿里后端定位）。

## 5. Plan B（工单否决恢复路径时）：小 batch 慢滴重推

15 抽样子集试点（有完整盲基线）：`rebuild_from_rds.py --docs <清单> --commit`（当日
PROD-RW）→ 笔记本 stage-3，`RAG_HA3_PUSH_BATCH_SIZE=16`，**每 doc 推完停 2–3 min**，
+30min/+2h/+24h 三点终验；试点活过 24h → 每小时 20–30 docs 分波滴完其余 470；试点被吃
→ 重推路线判死，只剩工单。**不走 DataWorks**（包旧无 [04b] 推后校验、claim 无节流、
日调度会自动吸走复位文档复现大窗口灌入）。
